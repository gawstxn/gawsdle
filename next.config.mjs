/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'enka.network', 
            'raw.githubusercontent.com', 
            'api.yatta.top', 
            'genshin.jmp.blue', 
            'keqingmains.com'
        ],
    },
};

export default nextConfig;
